// Package gpu provides the cgo bindings to libgnark_cuda used by the
// device-resident PLONK prover. The implementation is behind the cuda build tag
// (see gpu.go); this file keeps the package present without the tag.
package gpu
